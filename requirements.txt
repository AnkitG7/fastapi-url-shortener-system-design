# ======================================================
# requirements.txt — Project Dependencies
# ======================================================
# SYSTEM DESIGN CONCEPT: Dependency Management
# -------------------------------------------------
# In real systems, you pin exact versions to avoid
# "works on my machine" issues. This is part of
# building REPRODUCIBLE systems.
# ======================================================

# Core Framework
fastapi==0.115.0          # The web framework — handles HTTP, routing, validation
uvicorn[standard]==0.30.0 # ASGI server — the thing that actually RUNS your FastAPI app

# Database (Phase 2 — installing now so it's ready)
sqlalchemy==2.0.35        # ORM — maps Python classes to database tables
alembic==1.13.2           # Database migrations — version control for your schema
asyncpg==0.29.0           # Async PostgreSQL driver (used with SQLAlchemy)
psycopg2-binary==2.9.9    # Sync PostgreSQL driver (used by Alembic)

# Caching (Phase 3)
redis==5.1.0              # Redis client for Python

# Utilities
python-dotenv==1.0.1      # Load .env files into environment variables
shortuuid==1.0.13         # Generate short, unique IDs for our URLs
pydantic-settings==2.5.2  # Type-safe settings management
